Design reference files moved out of `src/app` so they do not add noise to the Next.js runtime tree.

- `stitch_cao_lanh_ward_civic_tourism_portal/`: archived HTML mockups
- `images/`: copied reference assets used by those mockups

These files are for design review only and are not part of the production app bundle.
